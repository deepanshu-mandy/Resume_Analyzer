[project]
name = "resume-analyzer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Deepanshu Mandhyan", email = "workofficial.deepanshu@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai>=3.6.0",
    "pypdf2>=3.0.1",
    "python-dotenv>=1.2.3",
    "streamlit>=1.62.0",
]

[project.scripts]
resume-analyzer = "resume_analyzer:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
